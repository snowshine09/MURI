<div id="map_dlg_{{id}}">
<<<<<<< HEAD
    <form class="form-inline" role="form">
    	<div id="map_selectbar_{{ id }}" class="selectbar form-group">
            <select class="selectpicker form-control" label="Other views...">
                <option value="">Generate the next linked window...</option>
                <optgroup label = "other visualizations...">
                    {% for vistype in vistypes %}
                    <option value = "{{ vistype }}_self">{{ vistype }}</option>
                    {% endfor %}
                </optgroup>
                <optgroup label = "Filter to a subset in ...">
                    <option value="map_subset">Map</option>
                </optgroup>
            </select>   
        </div>
        <div class="showcontext form-group">
             <button href="#" class="btn btn-sm btn-default" onclick="showcontext();return false;">Show Context</button>
        </div>
    </form>
	<div id="map_cvs_{{id}}" class="map cvs"></div>
    <div id="footprint_popup" class="hidden">
        <label class="header">ID: </label><span style="width:15px"></span><label id="footprint_id"></label><br>
        <label class="header">Name: </label><span style="width:15px"></span><label id="footprint_name"></label><br>
    </div>
</div>
=======
	<div id="map_selectbar_{{ id }}" class="selectbar dropdown">
        <select class="selectpicker form-control" label="Other views...">
            <option value="">Generate the next linked window...</option>
            <optgroup label = "other visualizations...">
                {% for vistype in vistypes %}
                <option value = "{{ vistype }}_self">{{ vistype }}</option>
                {% endfor %}
            </optgroup>
            <optgroup label = "Filter to a subset in ...">
                <option value="map_subset">Map</option>
            </optgroup>
        </select>   
    </div>
    <form class = "interaction">
        <div class="showcontext">
            {% if parent_id %}
            <a href="javascript:void(0)" onclick="showcontext(event);return false;" class = "form-control">Show Context in Link {{parent_id}}</a>
            {% endif %}
        </div>
    </form>
    <div id = "map_cvs_{{id}}" class = "cvs" style = "width: 100%; height: 100%">
    </div>
</div>                

>>>>>>> e6b183bd6ba7701e6d4abac79d36629d4b598acf
